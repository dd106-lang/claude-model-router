This directory holds a copy of Python's standard-library email package in ./email. Write a thorough unittest suite for email/quoprimime.py and email/base64mime.py: cover every public function, including edge cases such as empty input, line-length limits, and end-of-line handling. Put the tests in tests/test_quoprimime.py and tests/test_base64mime.py. Run them with python -m unittest discover -s tests and make sure they all pass. If a test fails because your expectation was wrong, correct the test; do not modify the package.
