# sample-repo

Tiny statistics package used to test tooling.

- Run the tests: `python -m unittest discover -s tests`
- Standard library only. No third-party packages.
- Tests use `unittest`, one `TestCase` class per function.
