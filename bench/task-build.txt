Add a percentile(values, p) function to stats.py that uses linear interpolation between closest ranks (p runs from 0 to 100; raise ValueError for p outside that range or for empty input), and rewrite median to use it. Add a --json flag to cli.py that prints the summary as a JSON object instead of text. The summary should now also include the median and the 90th percentile, in both the text and the JSON form; update report.py for that and keep the text lines consistent with the existing format. Add unittest coverage for percentile, for the new report fields, and for the JSON output. Make sure the whole suite passes before you finish.
