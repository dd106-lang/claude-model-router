Do not change any files. This directory is a copy of Python's standard-library email package. Explain how the package chooses the Content-Transfer-Encoding for a text body that contains non-ASCII characters when a message is created and serialized, under both the compat32 policy and the modern default policy. Trace the actual call path with file:line references, and then list every place in the package where base64 or quoted-printable is selected as an encoding, with file:line for each.
