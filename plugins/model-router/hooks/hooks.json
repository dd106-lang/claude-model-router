{
  "description": "Adds the model-router delegation policy to the lead's context when a session starts, resumes, clears, or compacts.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat \"${CLAUDE_PLUGIN_ROOT}/hooks/policy.md\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
